#pragma once

#include <color.hpp>

namespace algaw0lf::theme
{
    // colors
    static const NVGcolor W0LF_CYAN = nvgRGB(0x22, 0xe6, 0xef);
    static const NVGcolor W0LF_RED = nvgRGB(0xff, 0x32, 0x2c);

} // namespace w0lf